angular.module('app.controllers')

.controller('PlacesScheduleCtrl', [
    '$scope'
    '$rootScope'
    '$location'
    '$routeParams'
    'Schedule'
    '$timeout'
    '$q'

    ($scope, $rootScope, $location, $routeParams, Schedule, $timeout, $q) ->
      ""

  ])