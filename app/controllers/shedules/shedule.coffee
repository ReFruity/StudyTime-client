ScheduleCtrl = ($scope, $location, $routeParams, $rootScope, Sched, $route, Mistake, Group) ->

  # Set schedule

  # Get mistakes for show icon if have errors

  # Open/Close class details

ScheduleCtrl.$inject = ["$scope", "$location", "$routeParams", "$rootScope", "Sched", "$route", "Mistake", "Group"]